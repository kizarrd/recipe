extends layouts/main
include mixins/recipeBlock

block content 
    .recipes
        each recipe in recipes 
            +recipeBlock({
                id: recipe.id,
                imageFile: recipe.fileUrl,
                title: recipe.title,
            })